spring.application.name=smartlaptop

# Prefix: Where the JSP files are stored inside webapp
spring.mvc.view.prefix=/WEB-INF/views/

# Suffix: The file extension to append
spring.mvc.view.suffix=.jsp

spring.datasource.url=jdbc:mysql://localhost:3306/smartlaptop
spring.datasource.username=root
spring.datasource.password=Mudra2006#
spring.jpa.hibernate.ddl-auto=update

# Generates SQL statements optimized for the target MySQL version
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
